# paths.config
# example for paths.config, modify the following paths and rename this file to paths.config

# folder contains xlsx files
INPUT_DIR="../../tables"

# folder contains output scripts
OUTPUT_SRC_DIR="src/tables/gen"

# folder contains output data
OUTPUT_DATA_DIR="src/tables/gen"
